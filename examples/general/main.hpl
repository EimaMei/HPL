bool functionTest(string input) {
    if 30 > 50 || 30 == 320 || input == "if statement" {
        print("This gets printed because str equals to \"if statement\"")
        if true {
            print("This obviously gets printed as true equals to... true")

            if 4 == 2 {
                print("Doesn't get printed as 4 == 2 is false")
            }

            if false {
                print("Same story here")
            }

            if 30 < 40 {
                print("However, this gets printed!")
                input = "We also change the value of the variable since we can"
                input += " and plus it's funni."
            }
        }
        print(f"Output: {input}")

        return true
    }

    return false
    print("This should not get printed")
}

print("The function 'functionTest' has outputted a '" + functionTest(input = "if statement") + "', not something else.")

// Broken test code (ig conversion from bool to int doesn't work)
// int m = find(replaceAll(replaceAll("hey", "y", "qwe"), "qwe", "NYET"), "w")
// print(m)